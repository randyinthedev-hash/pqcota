# 암호 런타임 추상 원칙 — 계약이 어디까지 버티나 (검증 노트)

> **판정의 근거만 적는 문서다.** 새 암호 런타임을 코어 무변경으로 받는다는 계약(§0.5)에 가장 이질적인
> 후보 셋 — **정적 Go · Windows CNG · HSM** — 을 대입해 **어디가 부러지는지** 본 기록이다.
>
> 여기 있는 것은 종이 위의 판정이다. **무엇을 지원한다는 선언이 아니다** — 어떤 런타임을 지원한다고
> 말하려면 관측 코드와 실물 확인이 따로 필요하고, 그건 이 노트가 하지 않는다.

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 새 런타임을 넣으려면 **어디를 건드리나** | [CONTRIBUTING § 새 암호 런타임을 확장하려면](../CONTRIBUTING.md#새-암호-런타임을-확장하려면) — 결정 트리와 층별 파일 |
| 어느 릴리스에 **무엇이 들어가나** | [로드맵](../RELEASE_NOTES.md#로드맵--예정-릴리스-계획) |
| 지금 **서 있는 것** | [디스커버리](../discovery/디스커버리_설계.md) · [프로비저닝](../provisioning/프로비저닝_설계.md) 설계 |
| **왜 그렇게 판정했나** | 이 문서 |

> **§ 표기**: 별도 언급이 없으면 [프로세스 규정서](PQC플랫폼_단계별_프로세스규정.md)의 절 번호다.
> 이 노트가 계속 대조하는 **§0.5**는 「암호 런타임 추상 원칙」 — 새 런타임을 (a) 수집 · (b) 스키마 축 ·
> (c) remediation taxonomy 셋으로 받는다고 정한 절이다.

---

## 1. 계약이 조용히 요구하는 하중 가정 셋

§0.5는 축을 넷으로 적는다 — (a) 수집, (b) 스키마 축, (c) remediation taxonomy, (d) substrate.
그런데 코드는 **그 넷이 말하지 않는 것 셋**을 더 요구한다. 후보가 부러지는 자리는 언제나 축이 아니라
이 가정이다.

1. **provider 동형성** — "provider로 알고리즘 능력을 주입한다"가 성립해야 `PROVIDER_INJECT`·
   `CONFIG_ONLY`가 의미를 갖는다. 없으면 remediation은 `REBUILD` 한 방향으로 붕괴한다.
2. **POSIX 파일 substrate** — 아티팩트가 파일이고, 경로에 스테이징되고, 제거로 가역이다
   (`ModulePath` → `/opt/pqcota/*.so|*.jar`, Ansible `copy`, `state: absent`).
3. **관측 distinct** — 기존 collector가 이미 보는 것과 **다른 것**을 봐야 한다. 아니면 새 런타임이
   아니라 기존 런타임의 중복이다.

**가정 2가 이 노트의 발견이다.** 처음 §0.5는 축을 셋만 적었다 — openssl·jca가 **둘 다 POSIX
파일**이라 substrate가 `jca bool` 2-way 스위치 뒤에 숨어 있었기 때문이다. 여기서 드러난 뒤 (d)로
§0.5에 올렸다. 지금 규정서가 넷을 적는 것은 그 결과다.

---

## 2. 반례 셋 — 어느 가정에서 부러지나

### 2.1 정적 Go — 가정 1에서 부러진다

런타임 introspection 대상(provider)이 **없다.** 정적 ELF 심볼·`debug/buildinfo`로 `artifact`·
`symbol-analysis` 레인까지고(`inferred-low`, `confirmed` 아님), 협상 알고리즘은 netcap이 언어무관으로
와이어에서 이미 본다. provider가 없으니 remediation은 `REBUILD` 하나뿐이고, 배포할 파일 아티팩트도
없다(재빌드는 CI 몫).

**판정: 계약은 버티되 버팀의 방식이 "흡수"다.** 새 enum·render·substrate가 전부 불필요하다 — Go는
새 런타임이 아니라 **기존 taxonomy의 소비자**다.

### 2.2 Windows CNG — 가정 2에서 부러진다

수집은 완전 신규다(`BCryptEnumProviders`·레지스트리 — `/proc`·ELF·AF_PACKET 재사용 0). 스키마는
oneof라 순수 additive로 붙는다. Kind 어휘도 런타임 무관이라 **분류까지는** 재사용된다.

부러지는 곳은 아티팩트다. `Render`가 내야 하는 것이 `[openssl_init]` 텍스트가 아니라 레지스트리·GPO·
`Register-CngProvider`이고, 그건 곧 substrate다 — **render 층은 substrate와 분리되지 않는다.**

**판정: 절반만 얹힌다.** 디스커버리·인벤토리 절반은 지금 프레임에 그대로 오지만, 프로비저닝 절반은
substrate 일반화를 **선행 요구**한다. 그 일반화는 **Windows 구현이 손에 있을 때** 한다 — 구체 사례
없이 Registry/GPO 인터페이스를 미리 뽑으면 잘못된 seam을 뚫는다(투기적 추상화 금지).

### 2.3 HSM / PKCS#11 — 아무 가정도 건드리지 않는다

remediation이 대개 **openssl(`pkcs11-provider`)이나 jca(`SunPKCS11`)를 HSM으로 *가리키는*** 것이다.
렌더는 기존 `PROVIDER_INJECT`에 "이 provider의 target이 HSM 슬롯"이라는 파라미터를 얹는 일이고,
substrate(파일 스테이징 + config 조각)는 그대로 쓰인다.

**판정: 새 `CryptoRuntime` enum이 아니다.** 디스커버리 axis 신설 + provider가 가리키는 **backend
target**으로 모델링한다. 진짜 peer인 경우는 앱이 벤더 `.so`를 openssl/jca **없이 직접 링크**할 때뿐이고,
그마저 provider 동형 대상이라 openssl-계열 render로 근사된다.

> **정직성 각주(§2.6)** — HSM 하드웨어가 PQC를 아직 지원 안 하는 건은 억지 아티팩트를 지어내지 않고
> `DECOMMISSION`·`APP_RECONFIG` 주석으로 처리된다. taxonomy가 이미 이 비-config 종류를 갖고 있다.

---

## 3. 결론 — 부러지는 층은 하나다

| 후보 | 걸리는 가정 | 판정 |
|---|---|---|
| 정적 Go | 1 (provider 동형성) | `REBUILD` taxonomy로 **흡수** — enum 아님 |
| Windows CNG | 2 (POSIX 파일) | 별도 트랙 — substrate 일반화가 선행 |
| HSM/PKCS#11 | 없음 | 새 **axis + backend target** — enum 아님 |

### 판정을 결정 트리로

새 후보를 만났을 때 순서대로 묻는다. 하나라도 "아니오"면 새 1급 런타임이 아니다.

1. **관측이 기존 collector가 이미 보는 것과 다른가?**
   아니오 → **흡수**: 기존 런타임 finding에 `app_keys`·`usage_context`로 붙인다(새 enum 금지). *예: Python.*
2. **remediation이 provider 주입·config로 표현되나, 아니면 `REBUILD`뿐인가?**
   `REBUILD`뿐 → **taxonomy 흡수**, 새 render 불필요. *예: Go(가정 1).*
3. **배포 아티팩트가 POSIX 파일 + 경로 스테이징 + 파일 제거 롤백에 맞나?**
   아니오 → **substrate 일반화가 선행**돼야 하는 별도 트랙. *예: Windows(가정 2).*
4. **remediation이 기존 openssl/jca provider를 어떤 *target*으로 가리키는 것인가?**
   예 → 새 enum 아님, **backend target + 새 디스커버리 axis**. *예: HSM.*

넷을 다 통과(관측 distinct + provider-주입형 remediation + POSIX 파일 아티팩트 + 자체 provider 모델)해야
비로소 진짜 새 1급 `CryptoRuntime`이다. 그때 **건드릴 곳**은 [CONTRIBUTING](../CONTRIBUTING.md#새-암호-런타임을-확장하려면)에 층별로 있다.

**(a) 수집 · (b) 스키마 oneof · (c) taxonomy 어휘는 버틴다.** 셋 다 확장점으로 설계돼 있어 새 런타임을
코어 변경 없이 받는다.

**안 버티는 유일한 층은 (d) substrate의 "POSIX 파일" 가정**이고, non-POSIX에서만 부러진다. HSM은 파일을
재사용하고 Go는 배포물이 없어 둘 다 이 가정을 건드리지 않는다.

**흔히 다음 확장 대상으로 꼽히는 넷은 peer 후보가 아니다.** Python(링크한 libcrypto)·Node(대체로 OpenSSL
배달 차량)·.NET(하부 OS 따라 CNG 또는 OpenSSL)은 자기 substrate가 없어 openssl/CNG로 귀속되고, Go는
위 2.1대로 흡수된다. 진짜 새 substrate를 요구하는 건 **Windows CNG 하나**다.

---

## 부록 — 이 노트가 확정하지 않은 것 (열린 채로 둔다)

- **HSM axis의 실제 필드**(슬롯·모듈 경로·펌웨어 버전 등)는 실물 PKCS#11 관측 사례가 있을 때 정한다.
- **substrate 추상 인터페이스의 seam**(File-Stage vs Registry/GPO vs Config-Only).
- **착수 시점**: 위 어느 것도 실사용 수요·투자 결정 전에는 구현하지 않는다. 이 노트는 *"어떤 플랫폼이
  오든 계약이 버티는가"* 를 종이에서 검증했을 뿐이다.
